# HGVC

Hand Gesture Volume Controller using Python and computer vision.

## Features

* Real-time hand tracking
* Gesture-based volume control
* OpenCV visualization
* Media key volume control

## Tech Stack

Python, OpenCV, MediaPipe, NumPy, pynput

## Run

```bash
pip install -r requirements.txt
python main.py
```

Press `Q` to exit.
